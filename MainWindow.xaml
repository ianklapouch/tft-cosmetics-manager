<Window x:Class="tft_cosmetics_manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tft_cosmetics_manager"
        mc:Ignorable="d"
        Background="#24273a"
        Title="TFT Cosmetics Manager" Height="450" Width="500">
    <Grid>


        <Button 
            Name="randomizeButton" 
            Click="RandomizeButton_Click"
            Foreground="#cad3f5" Margin="151,188,151,188"
            Content="Randomize!"
            Cursor="Hand"
            >
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="#c6a0f6" BorderBrush="Gray" BorderThickness="1" CornerRadius="15">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>


        <Grid x:Name="Overlay" Background="#80000000" Visibility="Collapsed" Grid.ColumnSpan="2">
            <ProgressBar Width="200" Height="20" IsIndeterminate="True" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
